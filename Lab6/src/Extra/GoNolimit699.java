package Extra;

public class GoNolimit699 extends Promotion {
    public GoNolimit699() {
        setNet(6);
        setMincall(0);
        setMusic(true);
        setNolimitdtac(true);
        setWifi(true);
    }

    @Override
    public String getData() {
        return "Go No limit 699 "+'\n'+
               "No limit Internet 6 Mbps"+'\n'+
               "No limit to call to Dtac user"+'\n'+
               "No limit listen the online music"+'\n'+
               "No wifi limit"+'\n'+
               "--------------------------------------------"+'\n'
                ;
    }
}
